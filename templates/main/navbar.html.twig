<div class="navbar top-0 sticky bg-base-100">
  <div class="flex-1">
    <a class="btn btn-ghost text-xl">MATOS</a>
  </div>
  {% for category in categories %}
    <a href="#{{ category|lower }}" class="btn btn-ghost mx-1">{{ category }}</a>
  {% endfor %}
  <div class="flex-none gap-2">
    <div class="dropdown dropdown-end">
      <div tabindex="0" role="button" class="btn btn-ghost btn-circle avatar">
        <div class="w-10 rounded-full">
          <img alt="Tailwind CSS Navbar component" src="https://upload.wikimedia.org/wikipedia/commons/9/99/Exampleavatar.png" />
        </div>
      </div>
      <ul tabindex="0" class="mt-3 z-[1] p-2 shadow menu menu-sm dropdown-content bg-base-100 rounded-box w-52">
        <li><a>Se déconnecter</a></li>
      </ul>
    </div>
  </div>
</div>